#!/usr/bin/env sh

node --require ./backend/node_modules/tsx/dist/cjs/index.cjs scripts/security-guard.ts
status=$?

if [ "$status" -ne 0 ]; then
  echo "Pre-commit security checks failed."
  exit "$status"
fi

exit 0
